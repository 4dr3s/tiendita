packages:
  - apps/*
  - packages/*

# pnpm 10 ignores dependency build scripts by default. This keep-list was
# generated by create-next-app inside apps/web; it lives here because the
# workspace root is the only place pnpm reads it from.
# Revisit with `pnpm approve-builds` if image optimization or file watching breaks.
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
