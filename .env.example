# Work Unit 0.1 — the only variables consumed today. Both are optional:
# the API validates them with zod and falls back to the defaults below.
NODE_ENV=development
PORT=3001

# -----------------------------------------------------------------------------
# Future variables (Work Unit 0.2+). Listed here for visibility only — the API
# must boot without any of them; there is no database requirement in 0.1.
# -----------------------------------------------------------------------------
# DATABASE_URL=postgresql://tiendita:tiendita@localhost:55432/tiendita_v2
# REDIS_URL=redis://localhost:56379
# AUTH_SECRET=
# AUTH_PUBLIC_URL=http://localhost:3001
# TELEGRAM_BOT_TOKEN=
# OPENAI_API_KEY=
