/**
 * @tiendita/shared-domain
 *
 * Shared domain primitives for TIENDITA v2: value objects, invariants and
 * cross-app domain contracts.
 *
 * Intentionally empty in Work Unit 0.1 — the first value objects (starting
 * with the ID value object, once ADR-0001 exists) land in later work units.
 */
export const PACKAGE_NAME = "@tiendita/shared-domain";