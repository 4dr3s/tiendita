import { base } from "@tiendita/eslint-config";

// The shared domain package is pure TypeScript — the shared base config
// applies as-is, no framework-specific tuning needed.
export default [...base];